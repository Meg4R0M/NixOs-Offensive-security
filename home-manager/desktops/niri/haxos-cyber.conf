-- Veille cybersécurité FR (CERT-FR / ANSSI) — le widget central du bureau.
conky.config = {
    out_to_wayland = true,
    out_to_x = false,
    own_window = true,
    own_window_type = 'desktop',
    own_window_argb_visual = true,
    own_window_argb_value = 0,

    background = true,
    double_buffer = true,
    update_interval = 2.0,
    total_run_times = 0,

    use_xft = true,
    font = 'JetBrainsMono Nerd Font Mono:pixelsize=13',
    override_utf8_locale = true,
    draw_shades = false,
    draw_outline = false,

    alignment = 'bottom_left',
    gap_x = 40,
    gap_y = 30,
    minimum_width = 820,
    maximum_width = 860,

    default_color = '00ff41',
    color1 = '00ff41',
    color2 = 'ff2b2b',
    color3 = '1f6d2f',
}

conky.text = [[
${color2}╔══════════════════════════════════════════════════════════════════════════════════════════════════════╗${color}
${color2}║${color}   ${color1}󰒌${font Robotastic:pixelsize=14} CERT-FR / ANSSI${color}  -  veille cybersecurite (FR)${font}
${color2}╠══════════════════════════════════════════════════════════════════════════════════════════════════════╣${color}
${execpi 1800 cert-fr-news}
${color2}╚══════════════════════════════════════════════════════════════════════════════════════════════════════╝${color}
${color2}[ALE]${color} alerte    ${color1}[AVI]${color} avis/CVE    ${color1}[ZAT]${color} ZATAZ    ${color1}[ACT]${color} actu
]]
